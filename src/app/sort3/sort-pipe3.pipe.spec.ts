import { SortPipe3Pipe } from './sort-pipe3.pipe';

describe('SortPipe3Pipe', () => {
  it('create an instance', () => {
    const pipe = new SortPipe3Pipe();
    expect(pipe).toBeTruthy();
  });
});
