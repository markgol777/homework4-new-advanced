import { SortPipe2Pipe } from './sort-pipe2.pipe';

describe('SortPipe2Pipe', () => {
  it('create an instance', () => {
    const pipe = new SortPipe2Pipe();
    expect(pipe).toBeTruthy();
  });
});
